# nice-plug-csound - Helpers to use Csound in the nice-plug

It defines standard parameters for Csound, update cycle of the parameters
and audio processor. Audio processor uses Csound to process audio.
